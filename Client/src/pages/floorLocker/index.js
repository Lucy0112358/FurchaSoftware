import { memo, useCallback } from 'react'

const floorLocker = () => {
  // const onClick = useCallback(() => {
  //   console.log('floorLocker')
  // }, [])
  return (
    <div>floorLocker</div>
  )
}

export default memo(floorLocker)